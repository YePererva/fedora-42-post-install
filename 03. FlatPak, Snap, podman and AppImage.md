
```bash
sudo dnf install snapd flatpak flatseal fuse fuse-libs -y

# equvalent for "Enable third-party repositories" for flatpak
sudo flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

sudo ln -s /var/lib/snapd/snap /snap
sudo systemctl enable --now snapd.socket

sudo dnf up --refresh -y
```

## Podman and Docker

```bash
sudo dnf install podman -y
sudo dnf -y install dnf-plugins-core

sudo dnf config-manager addrepo --from-repofile="https://download.docker.com/linux/fedora/docker-ce.repo"

sudo dnf install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y
sudo systemctl enable --now docker
```

## To run some Windows-only applications

### Wine

```bash
sudo dnf config-manager addrepo --from-repofile=https://dl.winehq.org/wine-builds/fedora/43/winehq.repo

# might want to replace 43 with variable $releasever for easier dist-ugrade latter
# sudo sed -i 's/43/\$releasever/g' /etc/yum.repos.d/winehq.repo

sudo dnf up --refresh
sudo dnf install wine winetricks yad --allowerasing -y
```

### Bottles

```bash
sudo dnf install bottles -y
# now run bottles for initiall set-up
bottles
```